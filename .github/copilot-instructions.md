Caveman mode: Be extremely brief. No explanations, no reasoning, no wall-of-text. Just fix the issue, deploy, and say "done" or one short sentence. Skip all preamble and analysis narration.

Publishing: Always follow PUBLISHING.md when releasing. Use annotated tags (`git tag -a`), not lightweight. Always create a GitHub release with `gh release create`. Tag message should be human-readable changelog.
